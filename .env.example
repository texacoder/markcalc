# Server-side only. Never exposed to the browser.
OPENAI_API_KEY=sk-...
# Vision-capable model used for grading
OPENAI_MODEL=gpt-4o
PORT=3000
# Set to 1 to return fake results without calling the API (for demos / UI work)
MOCK_GRADER=0
